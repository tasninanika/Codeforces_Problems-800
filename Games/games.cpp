#include<iostream>
using namespace std;

int main(){
    int n, c = 0;
    cin >> n;

    int h[n], a[n];
    for(int i = 0; i < n; i++){
        cin >> h[i];
        cin >> a[i];

        if(a == ){
            c++;
        }
    }

    return 0;
}
