#include<bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;

    while (t--) {
        string a;
        cin >> a;

        string b = a;

        for (int i = 0; i < b.size(); ++i) {
            if (b[i] == 'p'){
                 b[i] = 'q';
            }
            else if (b[i] == 'q'){

    }

    return 0;
}
