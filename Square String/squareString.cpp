#include<iostream>
using namespace std;

int main(){
    int t;
    cin >> t;

    while(t--){
        string s;
        cin >> s;

        int right = s.size();

        for(int left = 0; left < right/2; left++){
            int arr1.push_back(left[i]);
        }
    }

    return 0;
}
